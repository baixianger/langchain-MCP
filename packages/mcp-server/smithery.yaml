# Smithery configuration file: https://smithery.ai/docs/config
startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for this MCP
    type: object
    properties: {}
    required: []
  commandFunction: |-
    (config) => ({ command: 'npx', args: ['langchain-mcp'] })
